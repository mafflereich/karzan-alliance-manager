rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 開發測試期間允許所有讀寫權限
    // 注意：正式上線前請務必修改為更安全的規則
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
